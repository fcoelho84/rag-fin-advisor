[project]
name = "rag-fin-advisor"
version = "0.1.0"
description = "RAG use case"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "fastapi[standard]>=0.141.1",
    "lxml>=6.1.3",
    "pymupdf>=1.28.2",
    "python-dotenv>=1.2.3",
]

[tool.fastapi]
entrypoint = "app.main:app"
